# this file contains additional python configuration information that may change depending on the platform
# currently, this is set up for test-exec to run on a development machine, but could be changed to work for a differnet platform 
# such as android without too many edits
DB_PATH = '../astro_weather.db'
BSP_PATH = '../de442s.bsp'